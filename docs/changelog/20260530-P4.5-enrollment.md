# P4.5 · 节点注册令牌 + 一键安装

日期：2026-05-30

## 需求
- 添加节点流程从"手动 scp 证书"改为"管理员加节点 → 复制一行命令 → ssh 粘贴 → 自动跑起来"
- 每个节点独立 mTLS 证书（CN=node_id），CA 签发，可单独撤销

## 子任务
- [x] 1 common：持久化 CA key + sign_node_cert(node_id) ✅ rcgen x509-parser
- [x] 2 migration 0006：node_enrollment_tokens 表（含 CASCADE 跟节点）✅
- [x] 3 models.rs：EnrollmentToken / EnrollRequest / EnrollResponse ✅
- [x] 4 api.rs：POST /api/nodes/:id/enrollment + POST /api/nodes/enroll ✅
- [x] 5 install.sh 静态端点（include_str! 内嵌）✅
- [x] 6 install.sh：兑换证书 + 写 env + systemd（或手动启动提示）✅
- [x] 7 前端：节点表单加 help 文案 + 创建后弹一键安装命令 + 重发令牌按钮 ✅
- [x] 8 端到端：安装脚本本地跑通完整流程 ✅

## 执行结果
- ✅ admin 加节点 → 自动生成 24h 一次性令牌
- ✅ install.sh 兑换证书：每节点专属证书（CN=node_id），CA 签发
- ✅ 节点启动 mTLS 握手成功，master 视为 healthy
- ✅ 令牌重用 → 401（一次性验证通过）
- ✅ 前端弹窗显示完整命令（含 master URL 自动填充 + 复制按钮）

## 安全模型最终态
- CA 私钥 `certs/ca-key.pem` 持久化在 master，权限 600
- 每节点独立 mTLS 证书，CN=zhuanfa-node-<id>
- 撤销：删除节点 → CASCADE 删令牌 → master 拒连
- 令牌一次性 + 24h 失效 + 一节点同时只一个活跃令牌

## 安全模型
- 令牌一次性 + 24h 失效
- 每节点独立证书，CN=node_id（攻破单节点不影响其它）
- master 校验：TLS 握手 + node_id 存在于 DB
- 撤销：Web 删节点 → DB 移除 → master 拒连

## 执行结果
（回填）
