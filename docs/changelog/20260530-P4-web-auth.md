# P4 · Web UI + 鉴权 + 拓扑编辑器

日期：2026-05-30

## 产品决策（用户定）
- 客户自助增删改（仅限自己的转发）
- 邀请码自助注册（admin 生成码，一次性使用）
- 完整可视化拓扑拖拽编辑器
- SLA 历史窗口：近 1 小时（探测点存 240 个/节点）

## 子任务
- [x] 4.1 用户/邀请码/转发归属数据模型 + argon2 哈希 ✅
- [x] 4.2 axum JWT 鉴权中间件 + 角色（admin/customer）✅ Claims/AdminClaims 提取器
- [x] 4.3 API 权限：admin 全权，customer 仅看/改自己 ✅ 11/11 端到端通过
- [x] 4.4 React + Vite + Tailwind 骨架 + 登录/注册页 ✅
- [x] 4.5 Admin：节点/转发/用户/邀请码 CRUD UI ✅
- [x] 4.6 ⭐ 拓扑编辑器（React Flow 可视化 + 跳-节点表单）✅
- [x] 4.7 SLA 看板（Recharts 折线图，节点延迟近1h）✅ /api/sla/samples
- [x] 4.8 Customer 视图复用同界面（按角色控制路由 + API 归属过滤）✅
- [x] 4.9 master 同进程托管前端（rust-embed 嵌入 web/dist）✅ 单二进制 30MB
- [x] 4.10 Workflow 对抗审查 + 修复（33 agent / 198万 token / 27→19 确认）

## Workflow 审查（33 agent / 198万 token / 6维度）
候选 27 → 确认 19 → 剔除 8

### 已修（17/19）
**Critical (2，同 bug)**: ForwardRow 补 owner_id 字段（误报"会 panic"，但加上更清晰，DTO 也暴露）
**High (3)**:
- register/login IP 维度限速（login 5/分钟、register 3/小时；内存版 RateLimiter）
- TopologyEditor admin 可编辑现有转发（disabled 改 readOnly + save() 加 PUT 分支）
- 后端 PUT /api/forwards/:id 端点
**Medium (6/8)**:
- JWT secret env 缺失时改 OsRng 32 字节随机
- AdminClaims 每次 DB 校验 user 存在 + 当前 role=admin（防 token 撤销/降级）
- admin_bootstrap 密码 ≥6 强制
- migration 0005: idx_forwards_owner_port UNIQUE + idx_probe_ts
- err() 统一脱敏：内部错误对外只回 "internal error"，详情写日志
- delete_node 检测 forwards JSON 引用，有引用拒绝 + 列出
- probe DELETE 用 OFFSET 边界替代 NOT IN 子查询
**Low (3/5)**: 404 加 Content-Type、owner_id=0 注释、auth.init 冗余不修

### 暂缓（2/19）— 记入 P5
- localStorage JWT XSS 风险：cookie 化需后端 Set-Cookie + 前端 credentials；当前私有部署，控制面 mTLS 主导
- probe_samples INSERT+DELETE 事务：影响极小，10-50 客户规模不必

### 验证矩阵（7/7 通过）
- bootstrap 短密码拒启动 ✅
- owner_id 出 DTO ✅
- 同 owner 同端口 409 ✅
- 节点有引用 409 ✅
- PUT 编辑 200 ✅
- 业务错误透传 / 内部错误脱敏 ✅
- 登录 5 次后限速 429 ✅

## 技术决策
- JWT (jsonwebtoken) + Argon2 密码哈希
- React 18 + Vite + shadcn/ui + Tailwind v4
- React Flow（@xyflow/react）拓扑编辑
- Recharts 图表
- rust-embed 编译期嵌入静态资源
- probe_samples 表存历史样本，节点每次探测追加 1 行，滚动删旧

## 执行结果
（回填）
